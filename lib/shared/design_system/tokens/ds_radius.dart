import 'dart:ui';

import 'package:nutri_calc/shared/design_system/utils/extensions/ext_num_screen_adapter.dart';

class DsRadius {
  static Radius get small => Radius.circular(16.r);
  static Radius get full => Radius.circular(999.r);
}
